---
tags:
  - Java
创建时间: 2026-07-28
---
***
### 一、注释

Java中有三种常见的注释写法：单行注释、多行注释和文档注释。
##### 1. **单行注释**
- 以`//`开头，注释内容在`//`后面，一直到该行结束。例如：
```Java
// 这是一个单行注释
```
##### 2. **多行注释**
- 以`/*`开头，`*/`结尾，注释内容在两个符号之间。例如：
```Java
/*
这是一个多行注释
可以写多行内容
*/
```
##### 3. **文档注释**
以`/**`开头，`*/`结尾，注释内容在两个符号之间，每行注释前都有一个`*`。文档注释是一种特殊的注释，用于生成API文档。例如：
```java
/**
 * 这是一个简单的Java HelloWorld程序。
 * 
 * @author ：张三
 * @version ：1.0
 */
public class HelloWorld {
  
  /**
   * 这是主方法，用于将消息“Hello, World!”输出到控制台。
   * 
   * @param args 未使用的命令行参数。
   */
  public static void main(String[] args) {
    System.out.println("Hello, World!");
  }
}
```

文档注释中可以使用特殊的标签来表示注释内容的含义，例如：

- @author：指定作者。
- @version：指定当前代码版本。

**文档注释在类和方法前使用**，可以为代码编写清晰、易懂、规范的API文档，方便其他开发人员阅读和使用。

使用`javadoc`命令生成API文档：

- 打开命令行工具，进入HelloWorld.java文件所在的目录。输入以下命令：
```bash
javadoc -author -version -d doc HelloWorld.java
```
- 执行命令后，`javadoc`会在当前目录下生成一个名为 “doc” 的文件夹，其中包含了生成的API文档。打开 “doc” 文件夹，找到 “index.html” 文件，用浏览器打开即可查看生成的API文档。
***
### 二、标识符、关键字、字面量

##### 1. **标识符**

- **标识符**是用来给变量、方法、类和包等命名的字符序列。程序员有权利进行自主命名，但是要遵守一些**规范**。

- Java中合法的标识符需要满足以下要求：

	1. 标识符可以由**字母**、**数字**、**下划线**(`_`)和**美元符号**（`$`）组成，不能含有其他符号。
	2. 标识符**不能以数字开头**。
	3. 标识符是**区分大小写**的，即Foo和foo是两个不同的标识符。

- 国内的开发规范一般遵循阿里巴巴开发规约

	1. 见名知意
	2. 驼峰式命名方式（由几个单词拼接而成）
	3. **类名**、**接口名**、**枚举**、**注解**：首字母大写，后面每个单词首字母大写。（StudentService，UserService）
	4. **变量名**和**方法名**：首字母小写，后面每个单词首字母大写。（doSome，doOther）
	5. **常量名**：全部大写，每个单词用下划线连接。（LOGIN_SUCCESS，SYSTEM_ERROR）
	6. **包名**：全部小写
##### 2. 关键字

- Java关键字是Java编程语言中预定义的具有特殊含义的保留字，这些保留字不能被用作标识符或变量名，而是在语法中有特定的用法和限制。

- Java关键字的种类有很多，包括基本数据类型关键字（如int、double、boolean等）、控制流程关键字（如if、for、while等）、访问权限关键字（如public、private、protected等）、类和对象关键字（如class、new、extends、super等）、异常处理关键字（如try、catch、finally等）等。不同的关键字有不同的作用和用法，程序员需要根据具体的需求选择合适的关键字来编写代码。
##### 3. 字面量

- Java中，字面量指的是在程序中直接使用的数据，字面量是Java中最基本的表达式，不需要进行计算或转换，直接使用即可。

- 例如：
	1. 整数型：10、-5、0、100
	2. 浮点型：3.14、-0.5、1.0
	3. 布尔型：true、false
	4. 字符型：'a'、'b'、'c'、'1'、'2'、'国'
	5. 字符串型："Hello"、"World"、"Java"、"你好呀"
***
### 三、数据类型、变量

##### 1. 数据类型

Java语言包括两种数据类型：

- 基本数据类型
	- 整数类型：包括**byte**、**short**、**int**和**long**四种类型，用于表示整数。
	- 浮点类型：包括**float**和**double**两种类型，用于表示带小数点的数值。
	- 布尔类型：**boolean**类型，只有true和false两个值，用于表示逻辑值。
	- 字符类型：**char**类型，用于表示单个字符，它是基于Unicode编码的。
- 引用数据类型
	- 类、接口、数组、枚举等。（除了8种基本数据类型之外，其他都是引用数据类型，**包括String**。）

| **数据类型** | **字节数** | **具体取值范围**                                 | **默认值**  |
| -------- | ------- | ------------------------------------------ | -------- |
| byte     | 1       | -128 ~ 127                                 | 0        |
| short    | 2       | -32768 ~ 32767                             | 0        |
| int      | 4       | -2147483648 ~ 2147483647                   | 0        |
| long     | 8       | -9223372036854775808 ~ 9223372036854775807 | 0L       |
| float    | 4       | 1.4E-45 ~ 3.4028235E38                     | 0.0f     |
| double   | 8       | 4.9E-324 ~ 1.7976931348623157E308          | 0.0d     |
| boolean  | 1       | true / false                               | false    |
| char     | 2       | 0 ~ 65535                                  | '\u0000' |

关于默认值：Java语言中变量必须先声明，再赋值，才能使用。
- 对于**局部变量**来说必须手动赋值，对于**成员变量**来说，如果没有手动赋值，会自动赋默认值。

###### 1.1 数据类型转换规则

- 小容量转换为大容量，叫做**自动类型转换**，容量从小到大的排序为：
	- byte < short(char) < int < long < float < double
	- 注意char比short可以表示更大的整数 
```java
double a = 10;      // 将int类型自动转换为double类型
int b = 100;
float c = b;        // 将int类型自动转换为float类型
long d = b;         // 将int类型自动转换为long类型
byte e = 10;
short f = e;        // 将byte类型自动转换为short类型
```

- 大容量转换为小容量，叫做**强制类型转换**。
- Java中大容量是无法直接转换成小容量的。因为这种操作可能会导致精度损失，所以**这种行为交给了程序员来决定**，当然这种后果自然是程序员自己去承担。因此在代码中需要程序员自己亲手加上强制类型转换符，程序才能编译通过。

- 以下语句若没有强制类型转换符则会编译时报错。
```java
int num = (int)10L;
```
- 强制类型转换时，底层二进制是如何变化的？**原则：砍掉左侧多余的二进制**。

###### 1.2 要点注意

- 整数的不同进制表示法
```java
int a = 10;// 以数字0-9组成的常数，默认为十进制表示法
int b = 0b101;// 以0b或0B开头的常数，由0和1组合而成
int c = 012;// 以0开头的常数，由数字0-7组成
int d = 0x1F;// 以0x或0X开头的常数，由0-9和A-F（大小写均可）组成
```

- Java中整数型字面量默认被当做int类型来处理，**如果要表示long类型的整数，需要在字面量后面加上'L'或'l'标记**。例如，下面是表示int和long类型整数的字面量的示例：
```java
int x = 10; // 10是一个int类型的字面量
long y = 10L; // 10L是一个long类型的字面量 ***
long z = 2147483648; // 编译时报错
long z = 2147483648L; //正确写法
```

- 整数字面量如果**没有超出**byte short char的取值范围，可以**直接赋值给byte short char类型的变量，无需强制类型转换**。
```java
byte b = 127; // 这是允许的
byte b = (byte)128; // 超出范围则要强制转换
char c = 97;
short d = 32000;
```

- 在Java中，多种数据类型混合运算时，**各自先转换成容量最大的类型**，再做运算。
```java
byte a = 100;
int b = 200;
long c = 300L;
long d = a + b + c; //如果d变量是int类型则编译器会报错
```

- 算式运算数字**都是字面量**，而非变量时，编译器可以**在编译阶段提前得出结果**
```java
byte x = 10 / 3; // 提前得出结果没有超过byte范围，直接赋值
int a = 10;
int b = 3;
byte x = a / b; // 编译报错，编译器只检测出结果是int类型
byte x = (byte)(a / b); // 强制类型转换
byte x = (byte)a / b; // 注意小括号的添加，只转换了变量a
```

- 浮点型字面量默认被当做double类型，**如果要当做float类型，需要在数字后面添加 F 或 f**
```java
float f = 3.0; // 编译报错
float f = 3.0F; 
```

- 浮点数的科学计数法
```java
double x = 0.123E2; // 0.123 * 10的平方
double y = 123.34E-2; // 123.34 / 10的平方
```

- 一些浮点数不能精确存储，因此**浮点数判断相等时不能使用常规方法**
```java
double x = 6.9;
double y = 3.0;
double z = x / y;
if(z == 2.3){  // 错误方法
    System.out.println("相等");
}
if(z - 2.3 < 0.000001){  // 正确方法
    System.out.println("相等");
}
```

- 给字符赋值空值
```java
char x = ''; // 编译器报错
char c = '\u0000'; // 正确方法
```

##### 2. 变量

 - Java中的变量可以按照作用域的不同划分为以下几类：

	1. **局部变量**：定义在方法、语句块、形式参数中的变量。
	2. **成员变量**：定义在类中，但在方法之外的变量。
		- 静态变量：使用static关键字定义的变量。
		- 实例变量：没有使用static关键字定义的变量。

- 变量的声明